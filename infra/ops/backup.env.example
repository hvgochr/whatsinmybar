# Copy to /etc/whatsinmybar/backup.env (root-owned, mode 600).
BACKUP_DIR=/srv/backups/whatsinmybar
RETENTION_DAYS=7
# Required: dedicated SSH destination with encrypted storage, no real credentials here.
BACKUP_REMOTE=
